{
  "name": "nxtwave-online-lms",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered NxtWave Online Virtual Classroom & Learning Management System",
  "scripts": {
    "start": "cd backend && npm start",
    "install:all": "npm install --include=dev && cd backend && npm install --include=dev && cd ../frontend && npm install --include=dev",
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "seed": "cd backend && npm run seed",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently --raw \"npm run backend\" \"npm run frontend\"",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
